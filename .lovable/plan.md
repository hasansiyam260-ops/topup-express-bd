Plan:

1. Use the uploaded TOP-UP EXPRESS logo image as the source for the PWA/app icon.
   - Replace the current generated TU icon.
   - Keep it same-to-same from your uploaded logo.
   - Crop/pad it carefully so Chrome install popup e logo clear medium size e dekha jay.

2. Regenerate only these icon files from that uploaded logo:
   - `public/icon-512.png`
   - `public/icon-192.png`
   - `public/apple-touch-icon.png`
   - `public/favicon-32.png`

3. Fix manifest icon sizes if needed so browser/app install popup uses the correct files properly.

4. Set bottom “Install App” prompt to medium size, not too small and not too big.
   - Button medium.
   - Icon medium.
   - Close X thakbe.
   - Dismiss korle next visit/session e abar katte parbe.

5. No other site design, banner, category, admin, text, layout, or backend changes.